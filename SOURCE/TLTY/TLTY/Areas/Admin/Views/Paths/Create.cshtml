@model EntityModel.EF.Path

@{
    ViewBag.Title = "Tạo mới đường dẫn";
	Layout = null;// "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="modal fade" id="createModal" style="display: block !important;">
	<div class="modal-dialog modal-lg" style="overflow-y: initial !important;overflow-y: scroll; max-height:85%;  margin-top: 50px; margin-bottom:50px;">
		<div class="modal-content">
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal">
					<span aria-hidden="true">&times;</span>
					<span class="sr-only">Close</span>
				</button>
				<h4 class="modal-title">Tạo mới đường dẫn </h4>
			</div>
			@using (Html.BeginForm())
			{
				<div class="modal-body" style="height: 400px;overflow-y: auto;">
					@Html.AntiForgeryToken()
					<div class="form-horizontal">
						@Html.ValidationSummary(true, "", new { @class = "text-danger" })
							<div class="form-group">
								<label class="control-label col-md-3">Tên đường dẫn:</label>
								<div class="col-md-9">
									@Html.TextBoxFor(model => model.PathName, new { @class = "form-control", placeholder = "Nhập tên đường dẫn ở đây" })
									@Html.ValidationMessageFor(model => model.PathName, "", new { @class = "text-danger" })
								</div>
							</div>

							<div class="form-group">
								<label class="control-label col-md-3">Đường dẫn:</label>
								<div class="col-md-9">
									@Html.TextBoxFor(model => model.Link, new { @class = "form-control", placeholder = "Nhập đường dẫn ở đây" })
									@Html.ValidationMessageFor(model => model.Link, "", new { @class = "text-danger" })
								</div>
							</div>

							<div class="form-group">
								<label class="control-label col-md-3">Mô tả:</label>
								<div class="col-md-9">
									@Html.TextAreaFor(model => model.Description, new { @class = "form-control", placeholder = "Nhập mô tả đường dẫn ở đây" })
									@Html.ValidationMessageFor(model => model.Description, "", new { @class = "text-danger" })
								</div>
							</div>
					</div>
					<div class="clearfix"></div>
				</div>
				<div class="modal-footer">
					<div class="form-actions no-color text-center">
						<input type="submit" value="Tạo mới" class="btn btn-success" />
						<button type="button" class="btn btn-danger" data-dismiss="modal">Đóng</button>
					</div>
				</div>
			}
		</div>
	</div>
</div>